## Summary

<!-- What does this PR do? Why is it needed? -->

## Changes

- 

## Test plan

- [ ] All existing tests pass (`npm test` in `services/odax-chat`)
- [ ] Swift tests pass (`xcodebuild test` for macOS app)
- [ ] No secrets or personal data introduced (security tests cover this)
- [ ] Build succeeds (`npm run build` / `xcodebuild`)

## Screenshots (if UI change)

<!-- Attach before/after screenshots for visual changes -->
